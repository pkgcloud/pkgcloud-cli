#!/usr/bin/env node
var program = require('commander');

program
  .command('network-list', 'List networks')
  .command('network-create', 'Create new network')
  .command('network-destroy', 'Destroy a network')
  .command('port-list', 'List ports')
  .command('port-create', 'Create new port')
  .command('port-destroy', 'Destroy a port')
  .parse(process.argv);
